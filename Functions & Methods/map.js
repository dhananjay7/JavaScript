// map is a array method which is the same as forEach what it does is it can create a new array with the old array after some operations. 
 

let arr = [2,4,6,8];

let newArr = arr.map ((val) => {
    return val;
});

// Also it is the same properties like forEach for its value, position , array.